# Global tool installer for EF Core
# Run once to prepare for migrations
# dotnet tool install --global dotnet-ef

# Create migration (from TaxProcessor.Api folder)
# dotnet ef migrations add InitialCreate

# Apply migrations
# dotnet ef database update
